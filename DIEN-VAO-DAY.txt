╔══════════════════════════════════════════════════════════════════╗
║              VDEAR — PHIẾU ĐIỀN THÔNG TIN (dễ điền)                ║
╚══════════════════════════════════════════════════════════════════╝

HƯỚNG DẪN:
• Chỉ cần gõ nội dung của bạn sau dấu  →
• Chỗ nào chưa biết cứ để trống, mình xử lý sau.
• KHÔNG dán mật khẩu/secret cho ai qua chat công khai — điền vào file này
  trên máy bạn thôi.

Ví dụ:
   Tên miền của bạn:
   → vdear.io

Bộ dịch vụ của bạn: Cloudflare · Clerk · Supabase · Upstash · Resend ·
                    PostHog · Sentry · Pinecone · ProductBridge


════════════════════════════════════════════════════════════════════
 PHẦN 1 · 3 THỨ CẦN NHẤT (làm trước là mình bắt đầu được ngay)
════════════════════════════════════════════════════════════════════

1) CLOUDFLARE — tên miền + tăng tốc + bảo mật + nơi chạy web.
   Vào dash.cloudflare.com → mua/thêm tên miền (Cloudflare bán luôn).
   Tên miền của bạn (vd vdear.io):
   →

2) CLERK — để user đăng nhập/đăng ký.
   Vào clerk.com → tạo app → mục "API Keys", copy 2 dòng:
   Publishable key (bắt đầu pk_):
   →
   Secret key (bắt đầu sk_):
   →

3) SUPABASE — kho dữ liệu (lưu user, coin yêu thích, tín hiệu...).
   Vào supabase.com → tạo project → Settings → API, copy 3 dòng:
   Project URL (dạng https://....supabase.co):
   →
   anon key:
   →
   service_role key (cái này BÍ MẬT):
   →


════════════════════════════════════════════════════════════════════
 PHẦN 2 · THÔNG TIN THƯƠNG HIỆU (để web trông chuyên nghiệp)
════════════════════════════════════════════════════════════════════

Tên sản phẩm (mặc định là Vdear, đổi nếu muốn):
→

Một câu giới thiệu ngắn (slogan):
→

Email hỗ trợ khách (vd support@vdear.io):
→

Bạn đứng tên cá nhân hay công ty? (ghi tên + quốc gia — cần cho trang pháp lý):
→

Đã có logo riêng chưa? (Có = gửi mình / Chưa = mình thiết kế tạm):
→

3 điểm mạnh nhất của Vdear (mỗi dòng 1 ý):
→
→
→

Mạng xã hội nếu có (X / Telegram / Discord):
→


════════════════════════════════════════════════════════════════════
 PHẦN 3 · CÁC DỊCH VỤ CÒN LẠI (điền sau cũng được)
════════════════════════════════════════════════════════════════════

RESEND — gửi email (chào mừng, cảnh báo giá). (resend.com → API Keys)
   API key (re_...):
   →
   Email gửi đi (vd no-reply@vdear.io):
   →

UPSTASH — bộ nhớ đệm giúp web nhanh, đỡ nghẽn. (upstash.com → tạo Redis → REST API)
   REST URL:
   →
   REST Token:
   →

POSTHOG — xem người dùng thao tác gì trên web. (posthog.com → Project API Key)
   Project key (phc_...):
   →

SENTRY — tự báo khi web bị lỗi. (sentry.io → Project → Client Keys DSN)
   DSN (https://...):
   →

PINECONE — "bộ não" cho tính năng AI hỏi–đáp coin. (pinecone.io → API Keys)
   API key:
   →

AI (để làm tính năng hỏi–đáp coin):
   Anthropic API key (console.anthropic.com):
   →
   (hoặc) OpenAI API key (platform.openai.com):
   →


════════════════════════════════════════════════════════════════════
 PHẦN 4 · 1 CÂU HỎI MÌNH CẦN BẠN TRẢ LỜI
════════════════════════════════════════════════════════════════════

"ProductBridge" trong danh sách của bạn là dịch vụ gì / dùng để làm gì?
(mình chưa xác định được nó — bạn mô tả ngắn để mình đưa vào đúng chỗ)
→


────────────────────────────────────────────────────────────────────
XONG PHẦN 1 là nhắn mình một tiếng — mình sẽ dựng đăng nhập + lưu dữ liệu
và đưa web lên tên miền của bạn. Các phần sau bổ sung dần.
────────────────────────────────────────────────────────────────────
