node_modules/
.DS_Store
*.log
.vercel
# Delivery archives are build artifacts: regenerable, and large enough that
# committing them would bloat the history. Was pinned to one filename, so any
# other export name slipped through.
*.zip
__pycache__/
*.pyc

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# secrets
.env
.env.local
.env*.local

# offline test build
.test-build/

# sinh ra lúc build từ legacy-static/ (xem scripts/copy-static.mjs)
/public/
